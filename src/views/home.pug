extends base.pug
include mixins/video

block content
  nav
    ul
      li 
        if !fakeUser.status
          a(href='/login') Login
        else
          a(href='/logout') Logout
  h2 Welcome! you will see trending videos here!
  each video in videos
    +video(video)