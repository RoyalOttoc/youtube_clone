extends base.pug
include mixins/video

block content
  nav
    ul
      li 
        if !fakeUser.status
          a(href='/login') Login
        else
          a(href='/logout') Logout
   
  h1 home
  each video in videos
    +video(video)